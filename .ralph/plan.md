# Plan

## Commitment Task Pool
- [x] `story:scaffold` | priority=1 | size=2 | title=Electron + React + TypeScript project scaffold with build and test infra | outcome=A running Electron app with empty main window, overlay window shell, Tailwind + Framer Motion configured, Biome + Vitest green, electron-builder configured, preload script with context bridge, design tokens defined | acceptance=`pnpm biome check .` passes, `pnpm tsc --noEmit` passes, `pnpm vitest run` passes, Electron app launches with two BrowserWindows configured (main: visible standard; overlay: hidden frameless alwaysOnTop transparent), IPC bridge typed and importable | source=PRD.md Phase 0 (Scaffold), Launch Decisions | test_plan=Skeleton test: App.test.tsx renders without crashing; verify Biome and TypeScript pass
- [x] `story:sqlite-database` | priority=2 | size=1 | title=SQLite database module with migrations and seed data | outcome=SQLite via better-sqlite3 with WAL mode, idempotent runMigrations() creates contexts + interview_messages + settings tables, 2 seed contexts inserted on empty DB for dev | acceptance=db-migration.test.ts: runMigrations() on in-memory DB creates all 3 tables, calling twice is idempotent, WAL mode enabled; seed data test: 2 complete contexts exist after first migration on empty DB | source=PRD.md Phase 0 (SQLite database module) + Phase 1 (Seed data), Data Model section | test_plan=db-migration.test.ts (idempotency, WAL mode, table existence); db-contexts.test.ts (seed data insertion, CRUD)
- [x] `story:openai-client` | priority=3 | size=1 | title=OpenAI API client with keychain integration and retry logic | outcome=OpenAI client using official `openai` package with streaming support, reads API key from env var or macOS Keychain via keytar, 1 retry on 5xx/timeout, no retry on 4xx, timeout constants per step (interview 15s, synthesis 30s, generation 10s) | acceptance=Client initializes with env var key; retry logic unit tested with mock; keytar read/write functions exist | source=PRD.md Phase 0 (OpenAI API client), AI Reliability Patterns, Environment Variables | test_plan=Unit test: client initialization, retry behavior with mocked responses (5xx retries once, 4xx does not)
- [x] `story:fr1-tray-main-window` | priority=4 | size=1 | title=FR-1: Menu bar tray icon and Context Studio main window | outcome=Electron Tray with * icon in menu bar, click opens main window, app runs in background for Quick Invoke availability, main window shows placeholder "Your *" with "No contexts yet" | acceptance=Tray icon visible in menu bar; clicking opens main window; closing main window does not quit app; app remains in background | source=FR-1, PRD.md Phase 0 (Tray integration) | test_plan=Manual verification: tray icon appears, click opens window, close doesn't quit; unit test for tray setup logic if feasible
- [x] `story:fr5-global-shortcut` | priority=5 | size=1 | title=FR-5: Global keyboard shortcut registration and overlay positioning | outcome=Cmd+Shift+8 registered system-wide via Electron globalShortcut, on trigger gets cursor position via screen.getCursorScreenPoint() and shows overlay window positioned near cursor | acceptance=Shortcut triggers from any app; overlay appears near cursor position; accessibility permission requested on first use | source=FR-5, PRD.md Phase 1 (Global keyboard shortcut) | test_plan=Manual verification: shortcut triggers overlay from external app; unit test for shortcut registration and cursor position logic
- [x] `story:fr6-overlay-selector` | priority=6 | size=1 | title=FR-6 part 1: Quick Invoke overlay with context selector | outcome=Overlay window shows numbered contexts with titles and one-line summary previews, select by click or number key (1-9), Escape dismisses, click-outside dismisses, empty state directs to create a context | acceptance=ContextSelector renders numbered list from DB; click selects; number key selects; Escape hides overlay; empty state shows guidance message | source=FR-6, PRD.md Phase 1 (Overlay UI, Context loading) | test_plan=ContextSelector.test.tsx (renders contexts, click selects, number key selects); OverlayRoot.test.tsx (selecting state, escape dismisses); db-contexts.test.ts (get-all-contexts returns complete contexts)
- [x] `story:fr6-prompt-generation` | priority=7 | size=1 | title=FR-6 part 2: Prompt input, streaming generation, and overlay lifecycle | outcome=After context selection, prompt field auto-focuses ("What's this * for?"), Enter submits, AI generates text using Quick Invoke Generator with streaming tokens visible in overlay, generation completes and triggers text injection, overlay auto-dismisses | acceptance=PromptInput auto-focuses and submits on Enter; generation streams tokens to GenerationPreview; overlay transitions through selecting->prompting->generating->done states; auto-dismiss after injection | source=FR-6, PRD.md Phase 1 (Quick Invoke generation, Overlay lifecycle) | test_plan=PromptInput.test.tsx (auto-focus, submit, disabled during generation); generation-flow.test.ts (mock OpenAI, verify streaming); OverlayRoot.test.tsx (state transitions)
- [x] `story:fr7-text-injection` | priority=8 | size=1 | title=FR-7: Text injection via clipboard + paste simulation with fallback | outcome=Generated text copied to clipboard and Cmd+V simulated via osascript AppleScript, on failure fallback to clipboard with toast "Copied — paste with Cmd+V", focus returns to original app after injection | acceptance=Text appears at cursor in target app; fallback toast shown on injection failure; clipboard contains generated text in all cases | source=FR-7, PRD.md Phase 1 (Text injection) | test_plan=Unit test: injection module calls clipboard.writeText + execFile osascript; fallback path tested with simulated failure
- [x] `story:fr2-interview-ui` | priority=9 | size=1 | title=FR-2 part 1: Interview chat UI with message persistence | outcome="New *" button creates interview context, opening question "What's top of mind?" with example chips, chat UI with reflective layout (not chat bubbles), progress indicator "Question N of 5", messages persisted to SQLite after every turn, resume on app relaunch | acceptance=InterviewChat renders messages with progress; messages saved to interview_messages table after each turn; reopening app shows "Continue interview" for in-progress contexts; input disabled during AI response | source=FR-2, PRD.md Phase 2 (Interview chat UI, Interview persistence) | test_plan=InterviewChat.test.tsx (renders messages, progress, typing indicator, disabled input); db-messages.test.ts (message CRUD, ordering by step_index)
- [x] `story:fr2-interview-ai` | priority=10 | size=1 | title=FR-2 part 2: Interview turn generator and synthesis pipeline | outcome=Interview turn generator calls gpt-4o-mini with conversation history per agent-prompt.md, 5 follow-up questions generated one at a time, after 5th answer synthesis triggers automatically using gpt-4o, synthesis output parsed into context fields (title, what_makes_this_you, summary, full_context_block, dimensions_json, key_signals_json), context status transitions interviewing->synthesizing->complete, retry once on JSON parse failure | acceptance=Full interview flow: opening -> 5 follow-ups -> synthesis; all messages persisted; context status = complete; all synthesis fields populated; interview-state.test passes (step_index advances, cannot exceed 5) | source=FR-2, PRD.md Phase 2 (Interview turn generator, Context synthesis), AI System Design | test_plan=interview-flow.test.ts (full flow with mock API); interview-state.test.ts (step index, status transitions); synthesis-parsing.test.ts (valid JSON parse, malformed rejection)
- [x] `story:fr3-reveal-card` | priority=11 | size=1 | kind=design| skill=taste-skill| title=FR-3: Synthesis reveal card with progressive disclosure | outcome=After synthesis, reveal card shows with progressive fade-in: "What makes this *you*" first (large typography centered), then identity dimensions unfold, then full context block in styled card, then key signals with badges, "Save this *" CTA at bottom; visually distinct background; scroll if content exceeds viewport | acceptance=RevealCard renders progressive disclosure in correct order; "Save this *" visible after full reveal; visually distinct from rest of app; Framer Motion animations | source=FR-3, PRD.md Phase 3 (Reveal card), Design Direction | test_plan=RevealCard.test.tsx (progressive disclosure order, save CTA visible, click save calls handler)
- [x] `story:fr9-onboarding` | priority=12 | size=1 | title=FR-9: First-launch onboarding flow | outcome=Shown when no API key in Keychain AND no contexts: Step 1 "Welcome to Your *" explanation, Step 2 API key input saved to Keychain via keytar, Step 3 Accessibility permission request with explanation, Step 4 "Create your first *" starts interview; seed data gate: seeds only insert if no onboarding completed | acceptance=Onboarding shown on first launch; API key stored in Keychain (not plaintext); accessibility permission requested; completing onboarding starts interview; seed data skipped for real users | source=FR-9, PRD.md Phase 3 (Onboarding flow, Seed data gate) | test_plan=Manual verification of full onboarding flow; unit test for onboarding state detection logic
- [x] `story:context-list-detail` | priority=13 | size=1 | title=Context list and detail views in Context Studio | outcome=Context list shows all complete contexts as numbered cards (*N, title, first line of summary), click opens detail view, "New *" always visible, "Continue interview" card shown for in-progress contexts; detail view shows full context (title, reveal, summary, dimensions, context block, signals), read-only with delete + confirmation, back button | acceptance=ContextList renders numbered cards; click navigates to detail; detail shows all fields read-only; delete with confirmation removes context; "Continue interview" appears for interviewing contexts | source=PRD.md Phase 3 (Context list view, Context detail view) | test_plan=ContextList.test.tsx (renders contexts, empty state, "Continue interview" card); manual verification of detail view and delete flow

## Exploration Task Pool
- [x] `story:explore-streaming-latency` | title=Optimize perceived latency in invoke loop — instant visual feedback and reduced delays | acceptance=Thinking shimmer shown before first token; focus delay eliminated; tests pass | source=opp:streaming-perceived-latency (0.526 > 0.195 divergence threshold) | serves_building_toward=The Analysis identifies perceived latency as the silent killer. This story reduces structural delays (focus: 50ms→0ms via requestAnimationFrame) and adds visual pacing (thinking shimmer before first token) so the wait between Enter and first streamed token feels active, not dead. | test_plan=GenerationPreview.test.tsx (thinking shimmer state), PromptInput.test.tsx (auto-focus), full suite green | priority=1 | size=1 | outcome=Deliver one concrete, loop-sized improvement
- [x] `story:explore-keyboard-flow-optimization` | title=Optimize keyboard-only invoke flow for sub-3s completion | acceptance=User can complete Cmd+Shift+8 -> number key -> type prompt -> Enter -> text at cursor without touching mouse, total time under 3 seconds with measured timings | source=Building Toward Analysis: "Number-key selection is what separates fast from magic" | serves_building_toward=The Analysis identifies the keyboard-only path (Cmd+Shift+8 -> 1 -> prompt -> Enter) as the primary designed path that drops total invoke time under 3 seconds. This story measures and optimizes that path — focus timing, input handling latency, overlay show speed — to ensure it hits the magic threshold. | rationale=The invoke loop's magic depends on speed. The keyboard-only path is the fastest possible flow but needs measurement and potential optimization of overlay show time, focus management, and input handling to achieve sub-3s. | test_plan=Timed manual test: measure end-to-end keyboard-only invoke from shortcut press to text at cursor; target <3s total | priority=1 | size=1
- [x] `story:explore-reveal-fullscreen` | title=Full-screen reveal card ceremony after synthesis | acceptance=Synthesis completion transitions InterviewChat to a full-screen RevealCard view at Studio level; chat messages/header no longer visible during reveal; progressive disclosure animation has full viewport; test updated | source=opp:reveal-fullscreen (0.374 > 0.105 divergence threshold), Open Question #8 confirmed by walkthrough | serves_building_toward=The Analysis identifies earned-synthesis as P0 demo moment #2. The reveal card is the emotional payoff for completing 5 interview questions — it must feel like a ceremony, not a card tacked onto a scrollable chat. | test_plan=InterviewChat test updated: verifies onSynthesisComplete callback called instead of inline RevealCard. Full suite 155/155. Biome + tsc clean. | priority=1 | size=1 | outcome=Deliver one concrete, loop-sized improvement
- [ ] `story:explore-overlay-spotlight` | title=Add Spotlight-native entry/exit animation to overlay | acceptance=Overlay content animates in with scale+fade on show (Framer Motion), phases transition smoothly, dismiss animates out; OverlayRoot tests updated; full suite passes; Biome + tsc clean | source=opp:overlay-spotlight-animation (0.192 > 0.099 divergence threshold), confirmed by iter 19 product walkthrough | serves_building_toward=The Analysis identifies overlay Spotlight-feel as critical for the P0 demo moment. The overlay window is correctly configured (frameless, transparent, alwaysOnTop) but content renders instantly. Adding motion makes the invoke loop feel like native OS infrastructure, not a web popup. | test_plan=OverlayRoot.test.tsx updated to verify Framer Motion wrapper renders; full suite green; Biome + tsc clean | priority=1 | size=1 | outcome=Overlay show/hide/phase transitions animate smoothly like Spotlight
- (none)

## Reprioritization Notes
- Last reprioritized iteration: 20
- Why the current top tasks are on top:
  - Scaffold must come first because the repo is greenfield — no Electron, no React, no build tooling exists yet
  - SQLite and OpenAI client are infrastructure dependencies for all FR stories
  - FR-1 (tray) establishes the app shell that all other UI lives in
  - FR-5 -> FR-6 -> FR-7 is the invoke loop chain ordered by dependency (shortcut -> overlay -> injection)
  - FR-2 (interview) depends on the AI client and DB being ready
  - FR-3 (reveal) depends on synthesis output from FR-2
  - FR-9 (onboarding) and context views are final polish that depends on everything above
  - This ordering matches the PRD's phase structure and demo priority (P0 invoke loop first)
